# Backend Environment Configuration Example with OAuth Mockups
# Copy this file to .env and update the values for your local development

# MongoDB connection URI
MONGODB_URI=mongodb://localhost:27017/financial_analyzer

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/financial_analyzer

# Server port
PORT=5000

# Node environment
NODE_ENV=development

# JWT Secret for authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Session secret (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=your-session-secret-here-also-change-in-production

# OAuth Configuration - Google (uncomment and configure to enable)
# To enable Google OAuth:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing one  
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add http://localhost:5000/api/auth/google/callback to authorized redirect URIs
# GOOGLE_CLIENT_ID=123456789-abcdefghijklmnop.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret-here

# OAuth Configuration - GitHub (uncomment and configure to enable)  
# To enable GitHub OAuth:
# 1. Go to GitHub Settings > Developer settings > OAuth Apps
# 2. Create a new OAuth App
# 3. Set Authorization callback URL to: http://localhost:5000/api/auth/github/callback
# GITHUB_CLIENT_ID=your-github-client-id-20-chars
# GITHUB_CLIENT_SECRET=your-github-client-secret-40-chars

# Frontend URL for OAuth redirects
FRONTEND_URL=http://localhost:3000

# Optional: CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# Optional: Rate limiting (requests per minute per IP)
# RATE_LIMIT=100

# Optional: File upload configuration
# MAX_FILE_SIZE=5242880
# UPLOAD_DIR=./uploads