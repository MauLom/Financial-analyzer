# Financial Analyzer Environment Configuration
# Copy this file to .env and update the values as needed for your local development environment

# ===========================================
# Backend Configuration
# ===========================================

# Server port (default: 5000)
PORT=5000

# Database configuration
# Path to SQLite database file (default: backend/data/financial.db)
# Uncomment and modify if you want to use a different database location
# DB_PATH=./backend/data/financial.db

# Node environment (development, production, test)
NODE_ENV=development

# ===========================================
# Frontend Configuration
# ===========================================

# API base URL for the React frontend
# This should point to your backend server
# Default: http://localhost:5000/api
REACT_APP_API_URL=http://localhost:5000/api

# ===========================================
# Development/Testing Configuration
# ===========================================

# Enable debug logging (optional)
# DEBUG=financial-analyzer:*

# SQLite database options (optional)
# SQLITE_MODE=OPEN_READWRITE_CREATE
# SQLITE_TIMEOUT=5000

# ===========================================
# Security Configuration (for future use)
# ===========================================

# JWT Secret (uncomment when authentication is implemented)
# JWT_SECRET=your-super-secret-jwt-key-here

# Session secret (uncomment when sessions are implemented)
# SESSION_SECRET=your-session-secret-here

# ===========================================
# API Configuration (for future use)
# ===========================================

# API rate limiting (requests per minute per IP)
# RATE_LIMIT=100

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# ===========================================
# File Upload Configuration (for future use)
# ===========================================

# Maximum file upload size in bytes (default: 5MB)
# MAX_FILE_SIZE=5242880

# Upload directory
# UPLOAD_DIR=./uploads